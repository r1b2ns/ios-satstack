# Fastlane sensitive credentials template.
# Copy this file to .env and fill in the real values:
#
#   cp fastlane/.env.template fastlane/.env
#
# .env is gitignored and must never be committed.

# ─── Apple ID ─────────────────────────────────────────────────────────────────
APPLE_ID=your.apple.id@example.com

# ─── App Store Connect team (numeric ID) ──────────────────────────────────────
# Required only when your account belongs to more than one team.
ITC_TEAM_ID=

# ─── App Store Connect API key (recommended over password) ────────────────────
APP_STORE_CONNECT_API_KEY_ID=XXXXXXXXXX
APP_STORE_CONNECT_API_ISSUER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
# base64-encode the .p8 file:  base64 -i AuthKey_XXXXXXXXXX.p8 | tr -d '\n' | pbcopy
APP_STORE_CONNECT_API_KEY_CONTENT=

# ─── Bundle identifiers ───────────────────────────────────────────────────────
BUNDLE_ID=com.example.app
BUNDLE_ID_DEV=com.example.app.dev
